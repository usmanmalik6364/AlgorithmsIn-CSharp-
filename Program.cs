using System;

namespace DynamicConnectivity
{
    class Program
    {
        static void Main(string[] args)
        {
            int N = Convert.ToInt32( Console.ReadLine()); //convert standard input to Integer
            //Uf uf = new Uf(N);
            //while (!Convert.ToInt32(Console.ReadLine()).isEmpty())
            //{

            //}

        }
    }
}
